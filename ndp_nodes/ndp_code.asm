import math

print(rf"Hello World, I am NDP '{node.id}' at '{node.storage_node}'")
a = {node.id}[0]
b = {node.id}[1]
result = math.sqrt(a**2 + b**2)


#later INST.SET 0 1 # set register[0] to 1
#later INST.SET 1 2 # set register[1] to 2
#later INST.ADD 0 1 2 # add register[0] to register[1]
#later INST.LOG "Hello" "World"

